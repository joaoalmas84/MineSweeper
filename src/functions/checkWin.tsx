import {Celula} from "../interfaces/celula.interface";

function checkWin(board:Celula[][]):boolean {
    const nLin = board.length;
    const mCol:number = board[0].length;
}

export default checkWin;